'use client';

import { Card, CardContent } from '@/components/ui/card';
import React from 'react';

const ChatPage = () => {
	return (
		<Card className='bg-bgr-base border-0 flex-center h-full w-full'>
			<CardContent>Select / Start a chat to get started!</CardContent>
		</Card>
	);
};

export default ChatPage;
